# TimeTracker Windows Agent - Required Dependencies

# Windows API access (REQUIRED)
pywin32>=305

# Process information
psutil>=5.9.0

# HTTP requests
requests>=2.31.0

# System tray GUI
pystray>=0.19.4
pillow>=10.0.0

# Modern UI
customtkinter>=5.2.0
CTkMessagebox>=2.5

# Windows notifications with ACTION BUTTONS (replaces win10toast)
windows-toasts>=1.1.0

# Global hotkeys (Alt+Ctrl+T)
keyboard>=0.13.5

# Optional: Build standalone executable
# pyinstaller>=6.0.0